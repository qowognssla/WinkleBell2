<Page
    x:Class="WinkleBell2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinkleBell"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <CollectionViewSource x:Name="DeviceListSource"/>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition  Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Pivot Grid.Row="1" >
            <PivotItem Header="Play Mode">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Back Sound"  FontSize="22" FontWeight="Bold"  Foreground="Gray" />
                    <Grid Grid.Row="1" Margin="10" BorderThickness="2" BorderBrush="Gray">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <MediaPlayerElement Grid.Row="0" x:Name="mediaPlayer" AreTransportControlsEnabled="True"/>
                        <MediaPlayerElement Grid.Row="1" x:Name="mediaPlayer2" AreTransportControlsEnabled="True"/>
                        <MediaPlayerElement Grid.Row="2" x:Name="mediaPlayer3"   AreTransportControlsEnabled="True"/>
                    </Grid>
                    <Button Grid.Row="2" x:Name="StartBtn" Content="Start" HorizontalAlignment="Center" Width="100" Height="50" Click="StartBtn_Click"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="Settings">
                <ScrollViewer>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>

                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Serial Setting"  FontSize="22" FontWeight="Bold" Margin="0,15,0,0" Foreground="Gray"/>

                        <Grid Grid.Row="1" Margin="15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition />

                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Serial Name : " Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <ListBox Grid.Column="1" x:Name="ConnectDevices" ItemsSource="{Binding Source={StaticResource DeviceListSource}}" Background="Gray" MinHeight="100" Padding="0" Margin="5,0" MaxWidth="600" MaxHeight="200">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding InstanceId}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
                            <Button Grid.Row="1" Grid.Column="1" x:Name="ButtonConnectToDevice" Content="Connect" VerticalAlignment="Top" Margin="0,0,20,0" Click="ConnectBtn_Clicked" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <Button Grid.Row="2" Grid.Column="1" x:Name="ButtonDisconnectFromDevice" Content="Disconnect" VerticalAlignment="Top" Click="DisconnectBtn_Clicked" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <Button  Grid.Row="5" x:Name="SetBtn" Content="Set" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="25" FontWeight="Bold" Click="SetButton_Click" Margin="20" Width="150" Height="60"/>
                    </Grid>
                </ScrollViewer>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
